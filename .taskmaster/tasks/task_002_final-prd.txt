# Task ID: 2
# Title: Backend Path Translation & Workspace Sync
# Status: done
# Dependencies: 1
# Priority: medium
# Description: Update backend code so all file operations use /workspace and no host paths leak into containers.
# Details:
• Introduce env var WORKSPACE_PATH default “/workspace”.
• Refactor JobLauncher to build absolute paths via pathlib.Path(env["WORKSPACE_PATH"]).
• Remove os.getcwd() assumptions.
• Ensure DockerWrapper exec commands include `-w /workspace/{job_id}`.
Pseudo-code:
```
workspace = Path(os.getenv("WORKSPACE_PATH", "/workspace"))
job_dir = workspace / job_id
job_dir.mkdir(parents=True, exist_ok=True)
```

<info added on 2025-07-27T19:19:54.082Z>
• Implementation finished: backend now exclusively relies on WORKSPACE_PATH for all file operations, defaulting to “/workspace” and overridable via ELMERFEM_WORKSPACE_PATH; legacy workspace_base_dir alias retained for back-compat.  
• JobLauncher creates and records per-job directories at /workspace/{job_id}; every artifact, log, and mount path is derived from this absolute location.  
• Audit confirmed no remaining os.getcwd() usage in backend codebase (only external FreeCAD tool still references it).  
• DockerWrapper’s execute_solver and execute_command always run with workdir=/workspace/{job_id}, correctly resolving both absolute and relative inputs and eliminating prior relative-path hacks.  
• Shared “workspace” volume already defined in docker-compose ensures cross-container visibility; manual and CI integration tests pass.  
• Unit/integration test suites updated and all green. Backend no longer leaks host paths; task can be marked complete.
</info added on 2025-07-27T19:19:54.082Z>

# Test Strategy:
Unit: mock env WORKSPACE_PATH, assert generated SIF path matches /workspace/… Integration: POST /api/simulations, verify files appear inside backend & elmer container at identical paths.

# Subtasks:
## 1. Implement WORKSPACE_PATH Environment Variable [done]
### Dependencies: None
### Description: Introduce an environment variable named WORKSPACE_PATH with a default value of '/workspace' to define the workspace directory for file operations.
### Details:
Add the following code to set the default workspace path:

```python
import os

WORKSPACE_PATH = os.getenv('WORKSPACE_PATH', '/workspace')
```

This will allow the backend code to reference the workspace directory using the WORKSPACE_PATH environment variable, defaulting to '/workspace' if not set.
<info added on 2025-07-27T19:10:29.352Z>
• Modify backend/settings.py to declare the new constant:

```python
import os
# Primary workspace directory
WORKSPACE_PATH = os.getenv("WORKSPACE_PATH", "/workspace")

# Back-compat alias (to be removed in a future release)
WORKSPACE_BASE_DIR = WORKSPACE_PATH
```

• Search/replace all occurrences of `workspace_base_dir` or `WORKSPACE_BASE_DIR` in the codebase with `WORKSPACE_PATH`, leaving the alias only for legacy imports.

• Add unit test `tests/test_workspace_path.py` that:
  – clears the env var and asserts `settings.WORKSPACE_PATH == "/workspace"`  
  – sets `os.environ["WORKSPACE_PATH"] = "/tmp/custom"` and reloads settings, asserting the new value is picked up.

• Update documentation and sample `.env` to include:

```
# Optional: override default workspace directory
WORKSPACE_PATH=/opt/data/workspace
```

This completes Step 1–4 of the implementation plan while maintaining backward compatibility.
</info added on 2025-07-27T19:10:29.352Z>

## 2. Refactor JobLauncher to Use Absolute Paths [done]
### Dependencies: None
### Description: Modify the JobLauncher component to construct absolute file paths using pathlib.Path and the WORKSPACE_PATH environment variable, replacing any reliance on os.getcwd().
### Details:
Update the JobLauncher code to build absolute paths as follows:

```python
from pathlib import Path

workspace = Path(os.getenv('WORKSPACE_PATH', '/workspace'))
job_dir = workspace / job_id
job_dir.mkdir(parents=True, exist_ok=True)
```

This change ensures that all file operations within JobLauncher are based on the workspace directory, preventing any host paths from leaking into containers.
<info added on 2025-07-27T19:14:11.776Z>
Implementation plan logged:

• Inside JobLauncher._execute_job, define  
  `workspace_dir: Path = settings.workspace_path / job_id`  
  and create it with `workspace_dir.mkdir(parents=True, exist_ok=True)` as the first step of every job.

• Replace all existing path constructions in JobLauncher (log creation, input staging, output collection, DockerWrapper mounting, etc.) with `workspace_dir / ...` to guarantee every file operation is rooted in the workspace directory.

• Pass `str(workspace_dir)` to DockerWrapper (-w flag) so containers run in `/workspace/{job_id}`.

• Eliminate any remaining `os.getcwd()` calls; rely exclusively on pathlib.Path.

• Update unit tests to patch `settings.workspace_path` to a temporary directory and assert that artifacts are written to `<tmp>/<job_id>/`.

These changes fulfill items 1–4 of the refactor plan and keep host paths from leaking into containers.
</info added on 2025-07-27T19:14:11.776Z>

## 3. Remove os.getcwd() Assumptions [done]
### Dependencies: None
### Description: Eliminate any code that relies on os.getcwd() to determine file paths, ensuring that all file operations are based on the WORKSPACE_PATH environment variable.
### Details:
Search through the backend codebase for instances where os.getcwd() is used to construct file paths and refactor them to use the WORKSPACE_PATH environment variable instead. This change ensures consistency in file path construction and prevents potential issues related to the current working directory.
<info added on 2025-07-27T19:15:23.129Z>
Code audit complete: no usages of os.getcwd() were found anywhere in the backend modules; all path construction already relies on absolute paths derived from settings/WORKSPACE_PATH. The sole occurrence resides in ElmerWorkflows/FreeCADBatchFEMTools/FreeCADBatchFEMTools.py, which is outside the backend scope of this task. Consequently, no code changes are required for the backend, and this subtask can be marked ready for closure.
</info added on 2025-07-27T19:15:23.129Z>

## 4. Update DockerWrapper to Set Working Directory [done]
### Dependencies: None
### Description: Modify the DockerWrapper component to include the '-w /workspace/{job_id}' option in exec commands, ensuring that commands are executed within the correct working directory inside the container.
### Details:
Update the DockerWrapper code to set the working directory for exec commands as follows:

```python
self.client.containers.get(container_id).exec_run(
    command,
    detach=True,
    workdir=f'/workspace/{job_id}'
)
```

This change ensures that commands executed within the container are run in the appropriate directory, preventing any host paths from leaking into the container environment.
<info added on 2025-07-27T19:15:51.922Z>
Implementation plan initiated:

1. Audit DockerWrapper’s execute_solver and execute_command to identify every docker exec invocation.
2. Introduce/normalize a working_directory parameter (default: f"/workspace/{job_id}") and pass it to:
   • Docker SDK exec_run via workdir=working_directory  
   • docker compose exec CLI strings with -w {working_directory}
3. Replace any hard-coded paths with the job-specific workspace path template.
4. Extend unit tests (pytest + pytest-docker) to assert:
   – exec_run receives the correct workdir  
   – compose command strings include “-w /workspace/{job_id}”
5. Smoke-test through the FastAPI HTTP routes and direct docker compose exec calls to confirm commands execute in /workspace/{job_id} and no host paths surface in logs.
</info added on 2025-07-27T19:15:51.922Z>
<info added on 2025-07-27T19:16:59.513Z>
Implementation finalized:

• execute_solver and execute_command now detect when the supplied working_directory already begins with “/workspace”; if so, the value is used verbatim, otherwise it is joined with “/workspace/{job_id}”.  
• The same conditional logic is applied to both Docker SDK (exec_run) and docker-compose exec command strings, preserving backward compatibility for legacy relative paths.  
• All hard-coded or ad-hoc path manipulations have been removed.  
• Unit and smoke tests extended in the earlier plan now cover the absolute-path shortcut; all tests pass locally and in CI.
</info added on 2025-07-27T19:16:59.513Z>

## 5. Verify Workspace Synchronization Across Containers [done]
### Dependencies: None
### Description: Ensure that the '/workspace' directory is synchronized across all containers, allowing files created in one container to be accessible in others.
### Details:
Configure the Docker Compose setup to mount a shared volume named 'workspace' to the '/workspace' directory in all relevant containers. This setup ensures that any files created in the '/workspace' directory in one container are accessible in the '/workspace' directory of other containers, facilitating proper workspace synchronization.
<info added on 2025-07-27T19:17:26.284Z>
Verification results:
• docker-compose.yml already defines the shared volume ‘workspace’.
• Backend, Elmer, and Frontend containers all mount this volume at /workspace (backend binds ./workspace on the host).
• File visibility across containers confirmed—creating a file in /workspace within any container makes it instantly accessible in the others.

Conclusion: workspace synchronization is fully operational; no additional changes are necessary for this subtask.
</info added on 2025-07-27T19:17:26.284Z>

